# The repo was authored on Windows; without this, checking out on macOS/Linux and back
# leaves whole files showing as modified when only the line endings changed.
* text=auto eol=lf
*.png binary
*.jpg binary
*.jpeg binary
*.hbc binary
